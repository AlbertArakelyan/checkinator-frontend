import Success from './Success';

import useSuccessContainer from './useSuccessContainer';

const SuccessContainer = () => {
  useSuccessContainer();

  return <Success />;
};

export default SuccessContainer;
